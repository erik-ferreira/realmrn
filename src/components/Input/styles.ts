import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: 54,
    borderWidth: 1,
    borderColor: "#999",
    borderRadius: 8,
    padding: 16,
    color: "#FFF",
  },
})
